rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /memories/{memoryId} {
      allow read: if true;
      allow create: if true;
      allow update, delete: if true; // Admin gebeurt via verborgen code op de site. Voor hogere beveiliging later Firebase Auth gebruiken.
    }
  }
}
