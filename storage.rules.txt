rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /memories/{allPaths=**} {
      allow read: if true;
      allow write: if true; // Voor publieke uploads. Later beperken met Auth/App Check.
    }
  }
}
